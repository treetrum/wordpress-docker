{% set menu_pages = [18, 34, 35, 36] %}
{% for page in menu_pages %}
    <li>
        <a href="{{function('get_permalink', page)}}">
            {{function('get_the_title', page)}}
        </a>
    </li>
{% endfor %}
