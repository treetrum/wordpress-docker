<div class="hero-page {{class ?: ''}}">
    <div class="container">
        <div class="row">
            <div class="columns xs-12 md-8 lg-6">
                <h1 class="hero-page__title heading-1">
                    {{title ?: post.page_title_override ?: post.title}}
                </h1>
                <p class="hero-page__subtitle lead">
                    {{subtitle ?: post.page_blurb}}
                </p>
            </div>
        </div>
    </div>
</div>
